import 'package:flutter/material.dart';

class AppColors {
  // Clean Flat Design - Simple and Functional

  // Primary Colors - Clean Blues
  static const Color primary = Color(0xFF2196F3);     // Material Blue
  static const Color primaryDark = Color(0xFF1976D2);
  static const Color primaryLight = Color(0xFF64B5F6);

  // Accent Color
  static const Color accent = Color(0xFF4CAF50);      // Green for success/actions

  // System Colors
  static const Color success = Color(0xFF4CAF50);     // Green
  static const Color error = Color(0xFFE53935);       // Red
  static const Color warning = Color(0xFFFF9800);     // Orange
  static const Color info = Color(0xFF2196F3);        // Blue

  // Background Colors - Clean Light Theme
  static const Color backgroundLight = Color(0xFFFAFAFA);
  static const Color backgroundCard = Color(0xFFFFFFFF);
  static const Color backgroundElevated = Color(0xFFF5F5F5);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textMuted = Color(0xFF9E9E9E);

  // Border and Divider
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);
}
